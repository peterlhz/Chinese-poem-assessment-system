# System Architecture



Text Input
    │
    ▼
┌─────────────────────────────┐
│ EnhancedTextAnalyzer         │  LLM (DeepSeek/GLM) — scene segmentation,
│  • Scene segmentation        │  translation, cultural term interpretation,
│  • Ontological analysis      │  ontology construction, prompt compression
│  • Consistent prompt builder │
└──────────────┬───────────────┘
               ▼
┌─────────────────────────────┐
│ BayesianStableDiffusion      │  SDXL base + refiner, CLIP scoring,
│  • Guidance-scale BO tuning  │  multi-sample stochasticity control
│  • Multi-sample generation   │
└──────────────┬───────────────┘
               ▼
┌─────────────────────────────┐
│ ImageToVideoPipeline         │  SVD motion synthesis + SDXL img2img
│  • SVD motion synthesis      │  cross-scene transitions, subtitle overlay
│  • SDXL transition frames    │
└──────────────┬───────────────┘
               ▼
┌─────────────────────────────┐
│ Flask REST API + ngrok       │  Async task queue, progress tracking,
│  • /api/generate              │  video/image download endpoints
│  • /api/status/<task_id>      │
│  • /api/download/<task_id>    │
└─────────────────────────────┘
               ▼
        Android Client
